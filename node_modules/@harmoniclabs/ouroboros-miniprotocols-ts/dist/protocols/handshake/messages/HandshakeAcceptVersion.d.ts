import { CborString, CborArray, CanBeCborString, CborObj } from "@harmoniclabs/cbor";
import { VersionData } from "../HandshakeVersionTable/VersionData.js";
import { VersionNumber } from "../HandshakeVersionTable/VersionNumber.js";
export interface IHandshakeAcceptVersion {
    versionNumber: VersionNumber;
    versionData: VersionData;
}
export declare class HandshakeAcceptVersion implements IHandshakeAcceptVersion {
    readonly versionNumber: VersionNumber;
    readonly versionData: VersionData;
    readonly isN2N: boolean;
    constructor({ versionNumber, versionData }: IHandshakeAcceptVersion, n2n?: boolean);
    toCborBytes(): Uint8Array;
    toCbor(): CborString;
    toCborObj(): CborArray;
    static fromCbor(cbor: CanBeCborString): HandshakeAcceptVersion;
    static fromCborObj(cbor: CborObj): HandshakeAcceptVersion;
}
