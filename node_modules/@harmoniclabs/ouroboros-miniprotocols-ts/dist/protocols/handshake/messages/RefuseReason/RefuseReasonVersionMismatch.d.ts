import { CanBeCborString, CborArray, CborObj, CborString, ToCborObj, ToCborString } from "@harmoniclabs/cbor";
import { VersionNumber } from "../../HandshakeVersionTable/VersionNumber.js";
export interface IRefuseReasonVersionMismatch {
    validVersions: VersionNumber[];
}
export declare class RefuseReasonVersionMismatch implements ToCborString, ToCborObj, IRefuseReasonVersionMismatch {
    readonly validVersions: VersionNumber[];
    readonly isN2N: boolean;
    constructor(validVersions: VersionNumber[], n2n?: boolean);
    toCborBytes(): Uint8Array;
    toCbor(): CborString;
    toCborObj(n2n?: boolean | undefined): CborArray;
    static fromCbor(cbor: CanBeCborString, n2n?: boolean): RefuseReasonVersionMismatch;
    static fromCborObj(cbor: CborObj, n2n?: boolean): RefuseReasonVersionMismatch;
}
