import { CanBeCborString, CborArray, CborObj, CborString, ToCborObj, ToCborString } from "@harmoniclabs/cbor";
import { VersionNumber } from "../../HandshakeVersionTable/VersionNumber.js";
export interface IRefuseReasonRefuse {
    version: VersionNumber;
    errorMessage: string;
}
export declare class RefuseReasonRefuse implements ToCborString, ToCborObj, IRefuseReasonRefuse {
    readonly version: VersionNumber;
    readonly errorMessage: string;
    readonly isN2N: boolean;
    constructor({ version, errorMessage }: IRefuseReasonRefuse, n2n?: boolean);
    toCborBytes(): Uint8Array;
    toCbor(): CborString;
    toCborObj(): CborArray;
    static fromCbor(cbor: CanBeCborString, n2n?: boolean): RefuseReasonRefuse;
    static fromCborObj(cbor: CborObj, n2n?: boolean): RefuseReasonRefuse;
}
