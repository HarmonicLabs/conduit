"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.N2NMessageProposeVersion = void 0;
var cbor_1 = require("@harmoniclabs/cbor");
var N2NVersionTable_1 = require("../N2NVersionTable.js");
var N2NMessageProposeVersion = /** @class */ (function () {
    function N2NMessageProposeVersion(_a) {
        var versionTable = _a.versionTable;
        versionTable = (0, N2NVersionTable_1.isIN2NVersionTable)(versionTable) ? new N2NVersionTable_1.N2NVersionTable(versionTable) : versionTable;
        if (!(versionTable instanceof N2NVersionTable_1.N2NVersionTable))
            throw new Error("invalid verisionTable for 'N2NMessageProposeVersion'");
        Object.defineProperty(this, "versionTable", {
            value: versionTable,
            writable: false,
            enumerable: true,
            configurable: false
        });
    }
    N2NMessageProposeVersion.prototype.toCbor = function () {
        return cbor_1.Cbor.encode(this.toCborObj());
    };
    N2NMessageProposeVersion.prototype.toCborObj = function () {
        return new cbor_1.CborArray([
            new cbor_1.CborUInt(0),
            this.versionTable.toCborObj()
        ]);
    };
    N2NMessageProposeVersion.fromCbor = function (cbor) {
        return N2NMessageProposeVersion.fromCborObj(cbor_1.Cbor.parse((0, cbor_1.forceCborString)(cbor)));
    };
    N2NMessageProposeVersion.fromCborObj = function (cbor) {
        if (!(cbor instanceof cbor_1.CborArray))
            throw new Error("invalid CBOR for 'N2NMessageProposeVersion'");
        var _a = __read(cbor.array, 2), idx = _a[0], _versions = _a[1];
        if (!(idx instanceof cbor_1.CborUInt &&
            idx.num === BigInt(0)))
            throw new Error("invalid CBOR for 'N2NMessageProposeVersion'; invalid messge index");
        return new N2NMessageProposeVersion({
            versionTable: N2NVersionTable_1.N2NVersionTable.fromCborObj(_versions)
        });
    };
    return N2NMessageProposeVersion;
}());
exports.N2NMessageProposeVersion = N2NMessageProposeVersion;
