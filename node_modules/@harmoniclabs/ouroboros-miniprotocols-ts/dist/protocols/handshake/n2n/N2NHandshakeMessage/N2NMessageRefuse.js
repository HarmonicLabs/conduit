"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.N2NMessageRefuse = void 0;
var cbor_1 = require("@harmoniclabs/cbor");
var N2NRefuseReason_1 = require("../N2NRefuseReason/index.js");
var N2NMessageRefuse = /** @class */ (function () {
    function N2NMessageRefuse(_a) {
        var reason = _a.reason;
        if (!(0, N2NRefuseReason_1.isN2NRefuseReason)(reason))
            throw new Error("invalid reason for 'N2NMessageRefuse'");
        Object.defineProperty(this, "reason", {
            value: reason,
            writable: false,
            enumerable: true,
            configurable: false
        });
    }
    N2NMessageRefuse.prototype.toCbor = function () {
        return cbor_1.Cbor.encode(this.toCborObj());
    };
    N2NMessageRefuse.prototype.toCborObj = function () {
        return new cbor_1.CborArray([
            new cbor_1.CborUInt(2),
            this.reason.toCborObj()
        ]);
    };
    N2NMessageRefuse.fromCbor = function (cbor) {
        return N2NMessageRefuse.fromCborObj(cbor_1.Cbor.parse((0, cbor_1.forceCborString)(cbor)));
    };
    N2NMessageRefuse.fromCborObj = function (cbor) {
        if (!(cbor instanceof cbor_1.CborArray))
            throw new Error("invalid CBOR for 'N2NMessageRefuse'");
        var _a = __read(cbor.array, 2), idx = _a[0], _reason = _a[1];
        if (!(idx instanceof cbor_1.CborUInt &&
            idx.num === BigInt(2)))
            throw new Error("invalid CBOR for 'N2NMessageRefuse'; invalid messge index");
        return new N2NMessageRefuse({
            reason: (0, N2NRefuseReason_1.n2nRefuseReasonFromCborObj)(_reason)
        });
    };
    return N2NMessageRefuse;
}());
exports.N2NMessageRefuse = N2NMessageRefuse;
