import { CanBeCborString, CborArray, CborObj, CborString, ToCborObj, ToCborString } from "@harmoniclabs/cbor";
import { PeerAddress } from "../PeerAddress/PeerAddress.js";
export interface IPeerSharingResponse {
    peerAddresses: PeerAddress[];
}
export declare class PeerSharingResponse implements ToCborString, ToCborObj, IPeerSharingResponse {
    readonly peerAddresses: PeerAddress[];
    constructor({ peerAddresses }: IPeerSharingResponse);
    toCborBytes(): Uint8Array;
    toCbor(): CborString;
    toCborObj(): CborArray;
    static fromCbor(cbor: CanBeCborString): PeerSharingResponse;
    static fromCborObj(cbor: CborObj): PeerSharingResponse;
}
