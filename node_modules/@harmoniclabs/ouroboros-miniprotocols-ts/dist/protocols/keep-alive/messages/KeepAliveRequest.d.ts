import { CanBeCborString, CborArray, CborObj, CborString, ToCborObj, ToCborString } from "@harmoniclabs/cbor";
export interface IKeepAliveRequest {
    cookie: number | bigint;
}
export declare function isIKeepAliveRequest(stuff: any): stuff is IKeepAliveRequest;
export declare class KeepAliveRequest implements ToCborString, ToCborObj, IKeepAliveRequest {
    readonly cookie: number;
    constructor({ cookie }: IKeepAliveRequest);
    toJSON(): {};
    toJson(): {};
    toCborBytes(): Uint8Array;
    toCbor(): CborString;
    toCborObj(): CborArray;
    static fromCbor(cbor: CanBeCborString): KeepAliveRequest;
    static fromCborObj(cbor: CborObj): KeepAliveRequest;
}
