"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findSortedIndex = findSortedIndex;
exports.insertSortedHash = insertSortedHash;
function findSortedIndex(hashes, index) {
    var low = 0;
    var high = hashes.length;
    while (low < high) {
        var mid = (low + high) >>> 1;
        if (hashes[mid][1] < index)
            low = mid + 1;
        else
            high = mid;
    }
    return low;
}
function insertSortedHash(hashes, indexedHash) {
    void hashes.splice(findSortedIndex(hashes, indexedHash[1]), 0, indexedHash);
}
