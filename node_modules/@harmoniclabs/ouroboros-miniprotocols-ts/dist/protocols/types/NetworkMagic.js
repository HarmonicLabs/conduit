"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.forceNetworkMagic = exports.isNetworkMagic = void 0;
function isNetworkMagic(stuff) {
    return (typeof stuff === "number" && !Number.isNaN(stuff) &&
        Number.isSafeInteger(stuff) &&
        stuff >= 0 &&
        stuff <= 0xffffffff);
}
exports.isNetworkMagic = isNetworkMagic;
function forceNetworkMagic(stuff) {
    return Number(
    // Bigint operations because bitwise with number coerce to signed int
    BigInt(Math.round(Math.abs(Number(stuff)))) & BigInt("0xffffffff"));
}
exports.forceNetworkMagic = forceNetworkMagic;
